export interface Booklet {
    id: number;
    nama_booklet: string;
    url_booklet: string;
    // deadline?: string; // Optional, since it can be null
    created_at: string;
    updated_at: string;
}
